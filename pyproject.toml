[project]
name = "harness-agent-system"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.13"
dependencies = [
    "langchain>=1.3.1",
    "langchain-deepseek>=1.0.1",
]
